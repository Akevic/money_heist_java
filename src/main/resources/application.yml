spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/heist_java
    username: keva
    password: Sot80tolas
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    show-sql: true
    properties:
      javax:
        persistence:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-target: guru_database_create.sql
  h2:
    console:
      enabled: true